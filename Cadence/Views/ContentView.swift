//
//  ContentView.swift
//  Cadence
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color("BackgroundColor")
                .ignoresSafeArea()
            Text("Cadence")
                .foregroundColor(.white)
        }
    }
}
